# H2 In-Memory Database Configuration for Tests
spring.datasource.url=jdbc:h2:mem:goalcast_testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# JPA & Hibernate Configuration for H2
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# (Opsiyonel) Testler için farkl? bir JWT secret kullanabilirsiniz, bu canl? ortam? etkilemez
# app.jwtSecret=gizli-test-jwt-anahtari

# (Opsiyonel) Di?er test spesifik ayarlar
# logging.level.org.hibernate.SQL=DEBUG
# logging.level.org.springframework.test=DEBUG